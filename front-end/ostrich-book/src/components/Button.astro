<style is:global>
    button {
        cursor: pointer;
        background-color: var(--color-text);
        color: var(--color-bg);
        outline: none;
        border-radius: 4px;
        border: 1px solid var(--color-text);
        box-shadow: 4px 4px 1px rgba(226, 226, 226, 0.712);
        margin: .3rem 0;
        padding: .4rem .8rem;
    }
</style>